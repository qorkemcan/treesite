---
import fs from 'node:fs';
import path from 'node:path';
import { parse } from 'csv-parse/sync';

export async function getStaticPaths() {
  const filePath = path.resolve('./src/data/cities.csv');
  const fileContent = fs.readFileSync(filePath, 'utf-8');
  const records = parse(fileContent, { columns: true, skip_empty_lines: true, bom: true });
  return records.map((row: any) => ({
    params: { city: `tree-removal-${row.City.toLowerCase().replace(/\s+/g, '-')}` },
    props: { cityData: row },
  }));
}
const { cityData } = Astro.props as { cityData: any };
---
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover" />
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700;800;900&display=swap" rel="stylesheet">
    <title>{cityData.City} Tree Removal & Trimming | Emergency Services FL</title>
    <meta name="description" content={cityData.Description} />
    <style is:global>
      * { font-family: 'Inter', -apple-system, sans-serif !important; box-sizing: border-box; }
      body { margin: 0; background: #fcfcfc; color: #1a1a1a; line-height: 1.5; }
      header { background: linear-gradient(135deg, #1b4332 0%, #081c15 100%); color: white; padding: 5rem 1rem; text-align: center; border-bottom: 5px solid #f97316; }
      .hero-cta { background: white; padding: 2.5rem 1.5rem; border-radius: 24px; text-align: center; box-shadow: 0 20px 40px rgba(0,0,0,0.1); border: 1px solid #e5e7eb; margin: -3rem auto 4rem; max-width: 800px; position: relative; z-index: 10; }
      footer { background: #064e3b; color: #d1fae5; padding: 4rem 1.5rem 8rem; text-align: center; border-top: 1px solid rgba(255,255,255,0.1); }
      .footer-note { margin-top: 12px; font-size: 0.75rem; opacity: 0.9; font-style: italic; color: #d1fae5 !important; }
      .footer-note a { color: #d1fae5 !important; text-decoration: underline; }
      .sticky-btn-wrap { position: fixed; bottom: 0; left: 0; right: 0; background: rgba(255,255,255,0.95); backdrop-filter: blur(10px); padding: 12px 15px calc(12px + env(safe-area-inset-bottom)); z-index: 1000; display: flex; justify-content: center; border-top: 1px solid #eee; }
      .sticky-btn { background: #ea580c; color: white !important; width: 100%; max-width: 480px; padding: 16px; text-decoration: none !important; border-radius: 12px; font-weight: 800; text-align: center; font-size: 1.25rem; box-shadow: 0 4px 12px rgba(234, 88, 12, 0.4); display: block; }
    </style>
  </head>
  <body>
    <div style="background: #0d2818; color: #d8f3dc; padding: 12px; text-align: center; font-size: 0.85rem; font-weight: 600;">✓ LICENSED, INSURED & BONDED • 24/7 EMERGENCY RESPONSE</div>
    <header>
      <div style="max-width: 850px; margin: 0 auto;">
        <div style="font-size: 1.2rem; margin-bottom: 1.5rem; color: #ffb703;">⭐⭐⭐⭐⭐ Top-Rated Local Experts</div>
        <h1 style="margin: 0; font-size: clamp(2rem, 8vw, 3.6rem); letter-spacing: -1.5px; line-height: 1.1; font-weight: 800;">Expert Tree Removal & Services in {cityData.City}</h1>
        <p style="font-size: 1.3rem; margin-top: 1.5rem; color: #95d5b2; font-weight: 300;">Florida's Trusted Certified Arborists Since 2008</p>
      </div>
    </header>
    <main style="max-width: 1000px; margin: 0 auto; padding: 0 1.5rem;">
      <div class="hero-cta" style="margin-left: auto; margin-right: auto;">
        <h3 style="margin-top: 0; color: #064e3b; font-size: 1.8rem; font-weight: 800;">Need a Fast Estimate?</h3>
        <a href="tel:+18001234567" style="background: #ea580c; color: white !important; padding: 1.2rem 2rem; text-decoration: none; border-radius: 14px; font-weight: 900; font-size: clamp(1.3rem, 5vw, 2rem); display: inline-block; margin: 1rem 0; box-shadow: 0 10px 25px rgba(234, 88, 12, 0.4); border: 2px solid #fdba74;">📞 (800) 123-4567</a>
      </div>
      <section style="margin-top: 2rem;">
        <div style="background: white; border-radius: 20px; padding: 2.5rem; border: 1px solid #e5e7eb; line-height: 1.8;">
          <p style="font-size: 1.25rem; color: #374151; margin: 0; white-space: pre-line;">"{cityData.Description}"</p>
        </div>
      </section>
      <div style="margin-top: 4rem; text-align: center;">
        <img src="/team.jpg" alt="Tree Service Team" style="width: 100%; border-radius: 24px; box-shadow: 0 15px 35px rgba(0,0,0,0.1);" />
      </div>
    </main>
    <footer>
      <p style="margin: 0; font-size: 0.85rem; font-weight: 500; opacity: 0.9;">© 2026 ProTreeTrim™ Florida • Licensed, Bonded & Insured Specialists</p>
      <p class="footer-note">
        <a href="/about">About Us</a> • <a href="/privacy">Privacy Policy</a> • Partnerships: <a href="mailto:gorkemcan@aol.com">gorkemcan@aol.com</a>
      </p>
    </footer>
    <div class="sticky-btn-wrap"><a href="tel:+18001234567" class="sticky-btn">CALL FOR FREE QUOTE</a></div>
  </body>
</html>