---
// src/pages/join-network.astro (Kısmi güncelleme - Form alanı)
// ... [Üst kısımlar aynı]
---

<!-- Sadece güncellenen form kısmını veriyorum, geri kalanı aynı -->
<aside class="b2b-sidebar">
  <div class="form-wrapper">
    <h3 style="margin-top: 0; margin-bottom: 1.5rem; color: #1b4332; font-size: 1.4rem; font-weight: 800;">Inquiry Form</h3>
    <form action="/api/contact" method="POST" style="display: grid; gap: 15px;">
      <div style="display:none;" aria-hidden="true"><input type="text" name="fax_number" tabindex="-1" autocomplete="off" /></div>
      <input type="hidden" name="service" value="B2B-Partner-Application" />
      
      <div>
        <label class="form-label">Company Name</label>
        <input type="text" name="name" placeholder="Extreme Tree Services LLC" required class="form-input"/>
      </div>

      <div>
        <label class="form-label">Business Email</label>
        <input type="email" name="email" placeholder="owner@company.com" required class="form-input"/>
      </div>

      <div>
        <label class="form-label">Primary Service Area (County/City)</label>
        <input type="text" name="city" placeholder="e.g. Tampa / Hillsborough" required class="form-input"/>
      </div>

      <div>
        <label class="form-label">Phone Number</label>
        <input type="tel" name="phone" placeholder="(555) 000-0000" required class="form-input"/>
      </div>

      <div>
        <label class="form-label">Equipment & Crew Size</label>
        <textarea name="message" rows="4" placeholder="Describe your trucks, cranes, and crew capacity..." class="form-input" style="resize: vertical;"></textarea>
      </div>

      <button type="submit" class="submit-btn">SUBMIT PARTNERSHIP REQUEST</button>
    </form>
  </div>
</aside>